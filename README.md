# Easy SQL Show - CSV数据可视化工具

一个基于Flask的Web应用，能够自动将CSV文件转换为SQLite数据库，并提供实时数据可视化展示。支持中文列名和智能数据分组统计。

## 🚀 主要功能

### 1. 智能CSV处理
- **自动结构分析**：分析CSV文件结构并推断数据类型
- **动态表生成**：根据CSV内容自动生成SQLite表结构
- **数据导入**：将CSV数据自动导入到数据库中
- **中文列名支持**：完全支持中文列名，自动处理编码和SQL兼容性

### 2. 实时数据更新
- **文件变化检测**：使用MD5哈希值检测CSV文件变化
- **自动更新**：当CSV文件发生变化时自动更新数据库
- **性能优化**：避免不必要的重复处理

### 3. 智能数据分组统计
- **双重分组**：按数据集和评估指标进行双重分组
- **平均值计算**：自动计算分组后的数值列平均值
- **数据聚合**：智能识别数值列并进行统计计算

### 4. Web界面展示
- **数据表格**：以HTML表格形式展示数据库内容
- **实时刷新**：支持手动刷新数据
- **响应式设计**：适配不同屏幕尺寸
- **格式化显示**：数值自动格式化为两位小数

## 📁 项目结构

```
easy_sqlshow/
├── app.py                 # 主程序文件（包含所有功能）
├── static/
│   ├── data.csv          # CSV数据文件
│   ├── summary.db        # SQLite数据库文件
│   ├── summary.db.hash   # 文件变化检测哈希值
│   ├── style.css         # 样式文件
│   └── logo.png          # 网站Logo
├── templates/
│   └── index.html        # 网页模板
└── README.md             # 项目说明文档
```

## 🛠️ 安装和运行

### 环境要求
- Python 3.6+
- Flask

### 安装依赖
```bash
pip install flask
```

### 运行方式

#### 1. Web模式（推荐）
```bash
python app.py
```
- 启动Flask Web服务器
- 访问 http://127.0.0.1:5000 查看数据
- 自动检测CSV文件变化并更新

#### 2. 脚本模式
```bash
# 使用默认CSV文件
python app.py

# 指定CSV文件
python app.py ./static/your_file.csv

# 指定CSV文件和表名
python app.py ./static/your_file.csv YourTableName

# 指定CSV文件、表名和数据库路径
python app.py ./static/your_file.csv YourTableName ./static/your_db.db
```

## 📊 CSV文件格式

### 支持的列结构
- **固定列**（前5列）：
  - 第1列：`dataset` → 数据集
  - 第2列：`version` → 版本
  - 第3列：`metric` → 评估指标
  - 第4列：`parameter` → 参数
  - 第5列：`mode` → 模式
- **动态列**：第6列开始自动检测并添加额外的数据列
- **数据类型**：自动推断TEXT、INTEGER、DECIMAL等类型

### 中文列名支持
- **完全支持中文列名**：第5列之后可以使用中文列名
- **自动编码处理**：正确处理UTF-8编码的中文字符
- **SQL兼容性**：自动为中文列名添加双引号包围

### 示例CSV格式
```csv
dataset,version,metric,parameter,mode,准确率,测试分数,模型性能
ceval-computer_network,db9ce2,accuracy,文本问答,gen,68.42,63.41,85.5
ceval-operating_system,1c2571,accuracy,文本问答,gen,52.63,63.18,72.3
```

## 🔄 数据更新机制

### 自动更新
- 程序会自动检测CSV文件的变化
- 当文件内容发生变化时，自动重新处理数据
- 使用MD5哈希值进行高效的文件变化检测

### 手动刷新
- 访问 http://127.0.0.1:5000/refresh 强制更新数据
- 忽略文件变化检测，强制重新处理CSV文件

## 📈 数据分组统计功能

### 双重分组逻辑
1. **第一级分组**：按数据集名称进行分组
   - 自动提取数据集标识符（如 `ceval` 从 `ceval-computer_network`）
   - 支持多种命名模式的分割（下划线、连字符等）

2. **第二级分组**：按评估指标进行分组
   - 在数据集分组基础上，再按评估指标（如 `accuracy`、`score`、`f1`）分组
   - 每个分组组合计算独立的平均值

### 统计计算
- **数值列识别**：自动识别第6列开始的数值列
- **平均值计算**：对每个分组计算数值列的平均值
- **格式化显示**：数值自动格式化为两位小数

### 示例输出
```
数据集          | 版本   | 评估指标 | 参数   | 模式 | 准确率 | 测试分数
ceval          | db9ce2 | accuracy | 文本问答 | gen  | 65.42  | 58.86
ceval          | 1c2571 | score    | 文本问答 | gen  | 72.15  | 68.33
```

## 🎯 使用场景

### 1. 模型评估结果展示
- 展示机器学习模型的评估指标
- 支持多模型、多数据集的对比分析
- 按评估指标类型进行分组统计

### 2. 数据分析展示
- 将分析结果CSV文件转换为可视化表格
- 实时更新分析结果
- 支持复杂的数据分组需求

### 3. 数据监控面板
- 实时监控数据变化
- 提供直观的数据展示界面
- 支持多维度数据聚合

## ⚙️ 配置说明

### 文件路径配置
- **CSV文件**：默认 `./static/data.csv`
- **数据库文件**：默认 `./static/summary.db`
- **表名**：默认 `ModelEvaluation`

### 数据类型推断
- **数值型**：自动识别为DECIMAL(10, 4)
- **文本型**：自动识别为TEXT
- **日期型**：自动识别为DATETIME
- **科学计数法**：自动处理科学计数法格式的数值

### 列名处理规则
- **前5列**：转换为中文列名（数据集、版本、评估指标、参数、模式）
- **第6列开始**：保持原始列名，支持中文
- **特殊字符**：自动清理并转换为SQL兼容格式

## 🔧 高级功能

### 1. 智能列名处理
- **中文列名支持**：完全支持UTF-8编码的中文列名
- **自动清理**：清理特殊字符，保持SQL兼容性
- **双引号包围**：自动为中文列名添加双引号

### 2. 科学计数法处理
- **精度保持**：正确处理科学计数法格式的数值
- **范围检测**：自动检测数值范围，避免精度丢失
- **类型转换**：智能选择合适的数据类型

### 3. 空值处理
- **智能处理**：自动处理CSV中的空值和空字符串
- **类型转换**：根据列类型进行适当的空值处理

### 4. 错误处理
- **完善的异常处理**：详细的错误信息输出
- **文件检查**：自动检查文件存在性和格式正确性

## 📝 开发说明

### 核心函数
- `process_csv_to_database()`: 处理CSV到数据库的完整流程
- `generate_sql_table()`: 生成SQL表结构
- `import_csv_to_db()`: 导入CSV数据
- `get_grouped_statistics()`: 获取分组统计数据
- `render_grouped_table()`: 渲染分组统计表格

### 关键特性
- **动态SQL生成**：根据CSV结构动态生成SQL语句
- **中文编码处理**：完整的UTF-8编码支持
- **双重分组统计**：支持复杂的数据聚合需求
- **实时更新**：高效的文件变化检测和自动更新

### 扩展性
- 支持自定义CSV文件格式
- 可扩展的数据类型推断
- 灵活的数据库结构生成
- 可配置的分组统计规则

## 🚀 最新更新

### v2.0 新功能
- ✅ **中文列名支持**：完全支持中文列名，自动处理编码
- ✅ **双重分组统计**：按数据集和评估指标进行双重分组
- ✅ **科学计数法处理**：正确处理科学计数法格式的数值
- ✅ **格式化显示**：数值自动格式化为两位小数
- ✅ **性能优化**：改进的文件变化检测和数据处理

## 🤝 贡献

欢迎提交Issue和Pull Request来改进这个项目！

## 📄 许可证

本项目采用MIT许可证。

---

**注意**：这是一个开发服务器，不建议在生产环境中使用。生产环境请使用专业的WSGI服务器。 